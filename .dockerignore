node_modules
**/node_modules
**/dist
**/build
.git
.github
# legacy/ is INCLUDED in the build context — the server serves it as a
# static fallback at /legacy (see server/Dockerfile: COPY legacy ./legacy).
*.log
.env
.env.*
!.env.example
*.csv
.DS_Store
